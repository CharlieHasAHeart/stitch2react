{
  "name": "stitch2react",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "blueprint:generate": "npm run build && node dist/src/cli/generate-blueprint.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "openai": "^5.10.2",
    "zod": "^3.25.67",
    "zod-to-json-schema": "^3.24.6"
  },
  "devDependencies": {
    "@types/node": "^24.0.1",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
