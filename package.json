{
  "name": "stitch2react",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "blueprint:generate": "npm run build && node dist/src/cli/generate-blueprint.js",
    "stitch:generate": "npm run build && node dist/src/cli/generate-stitch.js",
    "generate:all": "npm run build && node dist/src/cli/generate-all.js"
  },
  "dependencies": {
    "@google/stitch-sdk": "^0.3.5",
    "dotenv": "^16.6.1",
    "openai": "^5.10.2",
    "rehype-parse": "^9.0.1",
    "rehype-stringify": "^10.0.1",
    "unified": "^11.0.5",
    "unist-util-visit": "^5.1.0",
    "yaml": "^2.9.0",
    "zod": "^3.25.67",
    "zod-to-json-schema": "^3.24.6"
  },
  "devDependencies": {
    "@types/node": "^24.0.1",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
